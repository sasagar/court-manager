{
  "name": "court-manager",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "oxlint --ignore-pattern='**/node_modules/**' --ignore-pattern='**/.wrangler/**' --deny=correctness --deny=suspicious api/src waku/src",
    "lint:fix": "oxlint --fix --ignore-pattern='**/node_modules/**' --ignore-pattern='**/.wrangler/**' --deny=correctness --deny=suspicious api/src waku/src"
  },
  "devDependencies": {
    "oxlint": "^1.37.0"
  }
}
